train:
  name: debug
  seed: 42
  device: auto
  precision: fp32

  optimizer:
    name: adamw
    lr: 0.0003
    weight_decay: 0.00001

  scheduler:
    name: none
    warmup_steps: 0

  steps:
    max_steps: 100
    log_every: 1
    save_every: 50
    eval_every: 50

  output_dir: runs/debug
