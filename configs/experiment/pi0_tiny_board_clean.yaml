defaults:
  - ../dataset/board_clean
  - ../model/pi0_tiny
  - ../train/default

experiment_name: pi0_tiny_board_clean

overrides:
  train:
    output_dir: runs/pi0_tiny_board_clean

  dataset:
    horizons:
      obs: 1
      action: 25
