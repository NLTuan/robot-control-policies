defaults:
  - ../dataset/board_clean
  - ../model/mlp
  - ../train/default

experiment_name: mlp_board_clean

overrides:
  train:
    output_dir: runs/mlp_board_clean

  dataset:
    horizons:
      obs: 1
      action: 25
